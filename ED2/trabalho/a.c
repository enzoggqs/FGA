#include <stdio.h>
#include <stdlib.h>

int main(){
  int tamPasso, n;
  int o[2], s[2];

  scanf("%d", &tamPasso);

  scanf("%d", &n);

  scanf("%d %d", &o[0], &o[1]);

  scanf("%d %d", &s[0], &s[1]);

  return 0;
}